dependencies:
  espressif/esp_hosted:
    version: "~0.0.9"
    rules:
      - if: "target in [esp32p4]"
  espressif/esp_wifi_remote:
    version: "~0.3.0"
    rules:
      - if: "target in [esp32p4]"
  esp_lcd_ili9341: "^1.0"
  esp_lcd_touch: ">=1.0.0"